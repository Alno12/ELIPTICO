dist/
node_modules/
test-results/
playwright-report/
public/sw.js
package-lock.json

# O Prettier não é aplicado ao código-fonte. Tentado durante a separação do
# App.jsx: ele reescreveu 466 linhas do stats.js, que nem fazia parte do
# trabalho, e explodiu a tabela de ZONES de cinco linhas alinhadas e legíveis
# para quarenta e seis. Formatação automática aqui custa mais do que rende.
src/


# a prosa dos documentos é quebrada à mão, por legibilidade
*.md

# metatags em ordem deliberada
index.html

# Gerados por `npm run arquitetura`. Formatá-los daria um diff a cada execução,
# e o formato deles é decidido pelo gerador, não pelo Prettier.
arquitetura.html
arquitetura.json
